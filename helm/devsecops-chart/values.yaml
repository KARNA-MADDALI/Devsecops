replicaCount: 2

image:
  repository: ghcr.io/karna-maddali/devsecops
  tag: sha-516217ecbc0c3cca5b8b5291c9254e9aa5ffeb6f
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  # If host is empty, Ingress will accept any host (useful for testing)
  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix
  tls: []
