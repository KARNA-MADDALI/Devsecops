apiVersion: v1
kind: Service
metadata:
  name: {{ include "devsecops-chart.fullname" . }}
  labels:
    app: {{ include "devsecops-chart.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "devsecops-chart.name" . }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
